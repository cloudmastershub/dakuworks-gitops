apiVersion: v1
kind: Service
metadata:
  name: dakuworks-backend
  namespace: dakuworks-dev
  labels:
    app: dakuworks
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: dakuworks
    tier: backend
