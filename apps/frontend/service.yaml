apiVersion: v1
kind: Service
metadata:
  name: dakuworks-frontend
  namespace: dakuworks-dev
  labels:
    app: dakuworks
    tier: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: dakuworks
    tier: frontend
